{% extends 'base.html.twig' %}

{% block title %}Hopital | Service{% endblock %}

{% block body %}
</br>
<h5 align="center">Service</h5>
<div class="col-xl-5 col-lg-6 col-md-8 col-sm-10 mx-auto text-center form p-4">
        <table class="table table-stripped">
                <thead class="thead-dark">
                <tr class="thead-dark">
                    <th>Id</th>
                    <th>Libelle</th>
                </tr>
            </thead>
            <tbody>
      <tr> 
        <td>{{ service.id }}</td>
        <td>{{ service.libelle }}</td>
         
                    </tr>
                </tbody>
        </table>
   
           

    <a href="{{ path('service_index') }}" class="btn btn-success">Voir Listes</a>

    <a href="{{ path('service_edit', {'id': service.id}) }}" class="btn btn-primary" >editer</a>

    {{ include('service/_delete_form.html.twig') }}
{% endblock %}
